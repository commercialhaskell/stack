spec-version: 0.36.0

name: myPackage

dependencies:
- base

library:
  source-dirs: src

executables:
  myExeA:
    source-dirs: appA
    main: Main.hs
    dependencies:
    - myPackage
  myExeB:
    source-dirs: appB
    main: Main.hs
    dependencies:
    - myPackage
