cabal-version: 2.0
name:          consumer-b
version:       0.1.0.0
build-type:    Simple

library
  exposed-modules: ConsumerB
  build-depends:
    base >= 4.7 && < 5,
    sig-pkg,
    impl-b
  mixins:
    sig-pkg requires (Str as Str)
  hs-source-dirs:  src
  default-language: Haskell2010
