spec-version: 0.36.0

name: myPackage
version: 0.1.0.0

dependencies:
- base

library:
  source-dirs: src

internal-libraries:
  internal:
    source-dirs: int

tests:
  test:
    source-dirs: test
    main: Main.hs
    dependencies:
    - internal
    - myPackage
