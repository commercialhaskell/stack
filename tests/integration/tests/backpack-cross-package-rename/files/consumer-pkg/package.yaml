spec-version: 0.36.0

name: consumer-pkg

dependencies:
- base

library:
  source-dirs: src
  dependencies:
  - name: sig-pkg
    mixin:
    - requires (Sig as Impl)
  - impl-pkg

executables:
  consumer-demo:
    source-dirs: app
    main: Main.hs
    dependencies:
    - consumer-pkg
