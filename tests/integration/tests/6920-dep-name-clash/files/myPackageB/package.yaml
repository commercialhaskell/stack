spec-version: 0.36.0

name: myPackageB

dependencies:
- base

verbatim: |
  foreign-library acme-missiles
    type: native-shared
    other-modules:
        Foreign
    build-depends:
        base
      , acme-missiles
    hs-source-dirs:
        src-foreign
    default-language: Haskell2010
    if os(Windows)
      options: standalone
