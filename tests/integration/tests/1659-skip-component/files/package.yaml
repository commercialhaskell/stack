spec-version: 0.36.0

name: myPackage
version: 0.1.0.0

dependencies:
- base

library:
  source-dirs: src

executables:
  myPackage:
    source-dirs: app
    main: Main.hs
  myPackage-failing:
    source-dirs: app-failing
    main: Main.hs

tests:
  failing-test:
    source-dirs: test
    main: Spec.hs

benchmarks:
  failing-bench:
    source-dirs: bench
    main: Bench.hs
