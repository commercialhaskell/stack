spec-version: 0.36.0

name: myPackage

dependencies:
- base

library:
  source-dirs: src

executables:
  myExe:
    source-dirs: app
    main: Main.hs
  myExe-failing:
    source-dirs: app-failing
    main: Main.hs

tests:
  test-failing:
    source-dirs: test
    main: Main.hs

benchmarks:
  bench-failing:
    source-dirs: bench
    main: Main.hs
