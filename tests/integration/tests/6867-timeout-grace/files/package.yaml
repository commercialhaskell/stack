name: timeout-grace-fixture
version: 0.1.0.0
license: BSD-3-Clause

ghc-options:
- -Wall

tests:
  timeout-grace-fixture-test:
    main: Main.hs
    source-dirs: test
    dependencies:
    - base
    when:
    - condition: '!os(windows)'
      dependencies:
      - unix
