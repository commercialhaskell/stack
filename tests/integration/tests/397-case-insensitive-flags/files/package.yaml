spec-version: 0.36.0

name: myPackage

flags:
  necessary:
    description: The package will not build unless this flag is true.
    manual: true
    default: false

dependencies:
- base

library:
  source-dirs: src
  when:
  - condition: flag(necessary)
    cpp-options: -DWORK
